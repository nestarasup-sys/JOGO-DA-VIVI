import {useGame} from '../store/gameStore';import {bg} from '../assets';
export function Episodes(){const g=useGame();return <div><Header title="Episódios" text="10 capítulos, replay livre e finais persistentes."/><div className="episodeGrid">{g.story.episodes.map(ep=><article className={ep.id>g.unlockedEpisode?'episodeCard locked':'episodeCard'} key={ep.id}><img src={bg(ep.cover)}/><div><small>EP {String(ep.id).padStart(2,'0')} • {ep.subtitle}</small><h3>{ep.title}</h3><p>{ep.desc}</p><button disabled={ep.id>g.unlockedEpisode} onClick={()=>g.setPlayer(ep.id)}>{ep.id>g.unlockedEpisode?'Bloqueado':g.completed.includes(ep.id)?'Rejogar':'Jogar'}</button></div></article>)}</div></div>};
export const Header=({title,text}:{title:string;text:string})=><div className="pageHead"><h1>{title}</h1><p>{text}</p></div>;
