import {Header} from './Episodes';import {useGame} from '../store/gameStore';
export function Settings(){const g=useGame();return <><Header title="Ajustes" text="Dados, manutenção e ferramentas de desenvolvimento."/><div className="settings"><article><h3>Autosave</h3><p>Persistência automática via Zustand/localStorage.</p><button onClick={()=>alert('Save já está atualizado.')}>Salvar agora</button></article><article><h3>Exportar save</h3><p>Baixe um backup JSON local.</p><button onClick={()=>{const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(useGame.getState(),null,2)],{type:'application/json'}));a.download='aurora-v3-save.json';a.click()}}>Exportar</button></article><article><h3>Reset</h3><p>Reinicia progresso, mantendo a V3 instalada.</p><button onClick={()=>confirm('Reiniciar campanha?')&&g.resetGame()}>Reiniciar</button></article></div></>}
