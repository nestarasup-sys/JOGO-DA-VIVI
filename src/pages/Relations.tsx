import {characters} from '../data/content';import {sprite} from '../assets';import {useGame} from '../store/gameStore';import {Header} from './Episodes';
export function Relations(){const g=useGame();return <><Header title="Relações" text="A afinidade é visível; a resposta certa não."/><div className="relationGrid">{(['gael','leon','ravi'] as const).map(id=><article className="relation" key={id}><img src={sprite(id,'neutral')}/><div><small>{g.route===id?'ROTA ATIVA':'INTERESSE ROMÂNTICO'}</small><h2>{characters[id].name}</h2><p>{characters[id].tag}</p><div className="meter"><i style={{width:(g.affinity[id]||0)+'%'}}/></div><b>{g.affinity[id]||0}%</b></div></article>)}</div></>}
