import {events} from '../data/content';import {useGame} from '../store/gameStore';import {Header} from './Episodes';
export function Events(){const g=useGame();return <><Header title="Eventos" text="Conteúdo opcional com amizade, romance e recompensas."/><div className="eventGrid">{events.map(e=><article className="event" key={e.id}><i>{e.icon}</i><small>DESBLOQUEIA NO EP {e.unlock}</small><h3>{e.title}</h3><p>{e.desc}</p><footer><span>✦ {e.reward}</span>{e.aff&&<span>♡ {e.aff}</span>}</footer><button disabled={e.unlock>g.unlockedEpisode||g.eventDone.includes(e.id)} onClick={()=>g.finishEvent(e.id,e.reward,e.aff)}>{g.eventDone.includes(e.id)?'Concluído':e.unlock>g.unlockedEpisode?'Bloqueado':'Participar'}</button></article>)}</div></>}
