import {messages} from '../data/content';import {useState} from 'react';import {Header} from './Episodes';
export function Phone(){const [active,setActive]=useState(Object.keys(messages)[0]);return <><Header title="Celular" text="Conversas desbloqueadas pela história e respostas de rota."/><div className="phone"><aside>{Object.entries(messages).map(([name,arr])=><button className={active===name?'active':''} onClick={()=>setActive(name)} key={name}><i>{name==='Sistema Aurora'?'✦':name[0]}</i><span><b>{name}</b><small>{arr.at(-1)}</small></span></button>)}</aside><section><header><i>{active[0]}</i><div><b>{active}</b><small>online recentemente</small></div></header><div className="chat">{messages[active as keyof typeof messages].map((x,i)=><p key={i}>{x}</p>)}</div><footer><button>Responder</button><button>Enviar coração ♡</button></footer></section></div></>}
