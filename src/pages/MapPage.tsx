import {locations} from '../data/content';import {useGame} from '../store/gameStore';import {Header} from './Episodes';
export function MapPage(){const g=useGame();return <><Header title="Mapa de Aurora" text="Locais desbloqueiam com a história. A primeira visita rende Lúmens."/><div className="map"><div className="river"/>{locations.map(l=><button key={l.id} disabled={l.unlock>g.unlockedEpisode} style={{left:l.x+'%',top:l.y+'%'}} onClick={()=>g.visit(l.id)} className="mapPin"><i>{l.unlock>g.unlockedEpisode?'🔒':l.icon}</i><b>{l.name}</b><small>{l.desc}</small></button>)}</div></>}
