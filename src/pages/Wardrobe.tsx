import {outfits} from '../data/content';import {sprite} from '../assets';import {useGame} from '../store/gameStore';import {Header} from './Episodes';
export function Wardrobe(){const g=useGame();return <><Header title="Guarda-roupa" text="Uma única moeda. Looks não bloqueiam a campanha."/><div className="wardrobe"><aside><img src={sprite('player','smile')}/><h2>{outfits.find(o=>o.id===g.equipped)?.name}</h2></aside><section>{outfits.map(o=><button className="outfit" key={o.id} onClick={()=>g.buyOutfit(o.id,o.price)}><i>{o.icon}</i><b>{o.name}</b><small>{g.ownedOutfits.includes(o.id)?g.equipped===o.id?'equipado':'seu':'✦ '+o.price}</small></button>)}</section></div></>}
