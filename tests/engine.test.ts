import {describe,it,expect} from 'vitest';import {resolveNode,progressPercent} from '../src/game/engine';describe('engine',()=>{it('resolves route variant',()=>{const n:any={type:'routeVariant',flag:'rain',variants:{gael:{text:'G'},leon:{text:'L'}}};expect(resolveNode(n,{rain:'leon'},null,'gael').text).toBe('L')});it('progress is bounded',()=>expect(progressPercent(15,10)).toBe(100))});
